Description:
    This generator will create all the necessary files to use and customize switch_user.

Example:
    rails generate user_switch:install

    This will create:
        config/initializer/switch_user.rb